<div class="modal fade" id="ModalNewTariff" tabindex="-1" role="dialog" aria-labelledby="ModalTariff" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="ModalTariff">Nuevo arancel</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>

            <div class="modal-body">
                <form>
                    <div class="form-group">
                        <label for="concept">Concepto</label>
                        <input type="text" class="form-control" id="concept" @bind="Tariff.concept" />
                    </div>
                    <div class="form-group">
                        <label for="amount">Monto</label>
                        <input type="number" class="form-control" id="amount" @bind="Tariff.amount" />
                    </div>
                    <div class="form-group">
                        <label for="dueDate">Vencimiento</label>
                        <input type="date" class="form-control" id="dueDate" @bind="dueDate" />
                        <text class="text-gray-800">No vence</text> <input type="checkbox" @bind="isOverdue"/>
                    </div>
                    <div class="form-group">
                        <label for="type">Tipo</label>
                        <select id="type" class="form-control" style="margin: 0px" @bind="Tariff.typeId">
                            @foreach (var option in TariffTypeItemList!)
                            {
                            <option value="@option.Id">@option.Name</option>
                            }
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="modality">Modalidad</label>
                        <select id="modality" class="form-control" style="margin: 0px" @bind="Tariff.educationalLevel">
                            @foreach (var option in modalityItemList)
                            {
                            <option value="@option.Id">@option.Name</option>
                            }
                        </select>
                    </div>
                </form>
            </div>

            <div class="modal-footer">
                <div class="right-container">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Cerrar</button>
                    <button class="btn btn-danger" @onclick="CreateTariffData">Guardar</button>
                </div>
            </div>

        </div>
    </div>
</div>